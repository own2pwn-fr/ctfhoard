# GitHub holds only code + the normalized catalog (text). The complete hard-copy
# corpus (data/corpus/, text + binary) lives on the Hugging Face dataset
# own2pwn-fr/ctfhoard-corpus (git-LFS), not here — so no Git LFS is needed on GitHub.
data/catalog/**  text
